services:
  app:
    image: ghcr.io/kirillbaranov/rag-knowledge-bot:latest
    pull_policy: always
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8001:8000"
    volumes:
      - ./data:/app/data
      - ./docs:/app/docs
